/**
 * GENERATED by scripts/resolve-canonical-editions.mjs — do not hand-edit.
 *
 * Map of `edition key → verified shamela.ws book_id`. This is the missing link
 * between the *scholarly* facts in `src/canonical-editions.mjs` (which
 * numbering authority is canonical) and the *live* shamela.ws identifiers.
 *
 * It is empty until the resolver has been run somewhere with network access to
 * shamela.ws — locally (`npm run resolve:canonical`) or in CI via the
 * `Refresh citation index` workflow, which commits the result back.
 *
 * Schema:
 *   {
 *     generated_at: string | null,     // ISO timestamp of the last resolve
 *     source: string | null,           // "https://shamela.ws"
 *     editions: {
 *       "<key>": { book_id, title, muhaqqiq, authorityRoman }
 *     }
 *   }
 *
 * NEVER assert a canonical flag from a title-only match — only book_ids that
 * the resolver confirmed at *authority* level (title + muhaqqiq) belong here.
 */
export default {
  generated_at: null,
  source: null,
  editions: {},
};
